---
# Sitemap metadata, configures the output of the sitemap.
# `base` and `url.loc` are required. `url.loc` is relative to `base`.

base: https://marcbperez.github.io/markdown-pages/
url:
  - loc: index.html
    lastmod: 2023-12-7
    changefreq: monthly
    priority: 0.5
  - loc: CHANGELOG.html
    lastmod: 2023-12-7
    changefreq: monthly
    priority: 0.5
---
